spring:
  main:
      allow-bean-definition-overriding: true
  cloud:
    config:
      fail-fast: true
      discovery:
        enabled: true
        service-id: config-server
      name: common,druid,multisourcehive,multisourcespark,multisourceoracle,multisourceoracleurl,multisourceoracleuser,multisourcemysql,multisourcemysqlurl,multisourcemysqluser
      profile: default,common,53,53,common,41,DAMSUITESTERTEST,common,10.7test,root
eureka:
  client:
    service-url:
      defaultZone: http://10.0.9.24:8761/eureka/,http://10.10.9.17:8761/eureka/
feign:
    hystrix:
      enabled: true
ribbon:
  eager-load:
    enabled: true
    clients: SERVICE-HADOOP53